@model eCommerce.Core.CommerceClasses.Shippers.Shipper

@{
    ViewData["Title"] = "AddShipper";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<div class="right_col" role="main">
    <div class="">
        <div class="">
            <div class="col-md-12 col-sm-12 col-xs-12">
                <div class="x_panel">
                    <div class="x_title">
                        <h2><i class="fa fa-bars"></i> Manage Payments</h2>
                        <div class="clearfix"></div>
                        <a href="@Url.Action("ManageShipper", "AdminTransaction")"><i class="fa fa-caret-left"></i> Back</a>
                    </div>
                    <div class="clearfix"></div>
                    <div class="x_content">
                        @using (Html.BeginForm("AddShipper", "AdminTransaction", null, FormMethod.Post, true, htmlAttributes: new { @class = "form-horizontal" }))
                        {
                            @Html.HiddenFor(model => model.Id)
                            <div class="form-group">
                                <div class="col-md-2">
                                    <label class="control-label">Shipper Name</label>
                                </div>
                                <div class="col-md-6">
                                    @Html.TextBoxFor(model => model.Nama, htmlAttributes: new { @class = "form-control shipper-field" })
                                </div>
                                <div class="col-md-2">
                                    <button type="submit" class="btn btn-primary" id="btn-submit-shipper">Submit</button>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript" src="~/js/AdminTransaction/admin-transaction.js"></script>

